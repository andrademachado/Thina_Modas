import styled from "styled-components";
import { cores } from "../../styles";


export  const Base = styled.div`
    background-color: ${cores.rosaBranco};
    width: 100%;
    
`
export const Categorias = styled.h5`
    display: flex;
    gap: 150px;
    margin-left: 25px;
    color: ${cores.vinho};
    letter-spacing: 1.8px;
`